export const CONTENT_TYPE_ID = "landingPage";
export const RECOMPUTE_DEBOUNCE_MS = 250;
export const APP_NAME = "Slug Smith";

// Locale policy: assuming single shared path for all locales (writes default locale only)
export const LOCALE_MODE: "single" | "per-locale" = "single";
